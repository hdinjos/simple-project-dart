dataType() {
  //return function must use return type
  // var val = 345;
  // print(val);
  // return val;

  //const and final variable;
  // task = 'makan';
  const bar = 100000;
  const atm = 1.01325 * bar;
  // final task;
  // task = 'minum';
  var foo = const [];
  foo = [1, 2, 3];
  print(foo);
  print(atm);

  var strShort = 'Kliwon';
  var strLong = 'Saya mau belanja di pasar ${strShort}';
  print(strLong);
}
